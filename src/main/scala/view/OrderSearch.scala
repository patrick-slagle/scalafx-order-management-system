import javafx.{fxml => jfxf, scene => jfxs}

/*
object OrderSearch extends JFXApp {
  val resource = getClass.getResource("/scala/order_search.fxml")
  if(resource == null) {
    throw new IOException("Could not load layout file.")
  }

  val root: jfxs.Parent = jfxf.FXMLLoader.load(resource)
  stage = new JFXApp.PrimaryStage {
    title = "Order Management System"
    scene = new Scene(root) {
      stylesheets =List(getClass.getResource("/scala/css/main.css").toExternalForm())
    }
  }
}
 */
